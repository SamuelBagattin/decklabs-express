apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "decklabs-express.fullname" . }}-test-connection"
  labels:
    {{- include "decklabs-express.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "decklabs-express.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
