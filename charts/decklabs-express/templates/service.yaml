apiVersion: v1
kind: Service
metadata:
  name: {{ include "decklabs-express.fullname" . }}
  labels:
    {{- include "decklabs-express.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "decklabs-express.selectorLabels" . | nindent 4 }}
